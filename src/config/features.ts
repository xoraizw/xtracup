// Temporary while the app is redesigned as a customer-only experience —
// flipping this back to true is the only step needed to restore the
// Staff/Owner shells and their entry points (StaffTabs/OwnerTabs and the
// "join as staff" flows are left intact, just unreachable while this is off).
export const STAFF_OWNER_POV_ENABLED = false;
